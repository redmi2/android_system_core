BUILD_COMPONENTS :=

if USELIBCUTILS
BUILD_COMPONENTS += libcutils
endif
BUILD_COMPONENTS += liblog logcat libzipfile

BUILD_COMPONENTS += adb libmincrypt fastboot usb fs_mgr mkbootimg

SUBDIRS := $(BUILD_COMPONENTS)
